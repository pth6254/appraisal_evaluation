
const screens=__SCREENS__;
const descriptions={appraisal:['AVM 실행 결과 · 실제 실거래 기반 참고 분석이며 감정평가가 아닙니다.','실제 AVM을 실행한 뒤 시세추정 결과가 반영된 래미안퍼스티지 84.9㎡ 후보 화면'],simulation:['실제 자금 계산 결과 · 대출·취득비용·필요 현금을 산출한 대시보드입니다.','실제 자금 시뮬레이션 결과 대시보드'],comparison:['후보 비교 · 자금 부족을 표시하고 시세·권리는 미확인 상태로 남깁니다.','후보 비교 화면: 필요 현금 30,960만원, 보유 현금 30,000만원, 부족 자금 960만원'],candidate:['재검토 안내 · 희망가 변경 후 저장된 자금 조건의 재확인을 안내합니다.','후보의 희망가 변경 후 자금 재검토 안내 화면']};
let selected='appraisal';const screen=document.getElementById('screen'); const positionScreen=()=>{document.querySelector('.viewport').scrollTop=selected==='comparison'?Math.round(screen.clientWidth*.31):selected==='appraisal'?Math.round(screen.clientWidth*.23):0;};screen.addEventListener('load',positionScreen);requestAnimationFrame(positionScreen);
document.querySelectorAll('[data-screen]').forEach(button=>button.addEventListener('click',()=>{selected=button.dataset.screen;screen.src=screens[selected];screen.alt=descriptions[selected][1];document.getElementById('screen-caption').textContent=descriptions[selected][0];document.querySelector('.viewport').scrollTop=0;document.querySelector('.viewport').setAttribute('aria-label',descriptions[selected][1]+', 세로 스크롤 가능');document.querySelectorAll('[data-screen]').forEach(b=>b.setAttribute('aria-selected',String(b===button)));}));
const slides=[...document.querySelectorAll('.slide')],nav=[...document.querySelectorAll('nav a')];let current=0;
function move(i){i=Math.max(0,Math.min(4,i));slides[i].scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'});}
function track(){let best=0;let distance=Infinity;slides.forEach((slide,i)=>{const d=Math.abs(slide.getBoundingClientRect().top-64);if(d<distance){best=i;distance=d}});current=best;nav.forEach((n,i)=>n.setAttribute('aria-current',String(i===current)));document.getElementById('position').textContent=String(current+1).padStart(2,'0')+' / 05';}
addEventListener('scroll',track,{passive:true});document.getElementById('prev').onclick=()=>move(current-1);document.getElementById('next').onclick=()=>move(current+1);
const modal=document.getElementById('modal'),body=document.getElementById('modal-body');
function zoom(){document.getElementById('modal-title').textContent=descriptions[selected][1];body.replaceChildren();const image=new Image();image.src=screens[selected];image.alt=descriptions[selected][1];body.append(image);modal.showModal();}
document.getElementById('zoom').onclick=zoom;screen.onclick=zoom;document.getElementById('close').onclick=()=>modal.close();
document.getElementById('fullscreen').onclick=async()=>{try{if(document.fullscreenElement)await document.exitFullscreen();else await document.documentElement.requestFullscreen()}catch{document.getElementById('fullscreen').textContent='브라우저 F11 사용'}};
addEventListener('keydown',e=>{if(modal.open||e.target.closest('button,a,input,textarea,[role=tablist]'))return;if(['ArrowRight','PageDown'].includes(e.key)){e.preventDefault();move(current+1)}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();move(current-1)}if(e.key==='Home'){e.preventDefault();move(0)}if(e.key==='End'){e.preventDefault();move(4)}});
document.getElementById('sources').onclick=()=>{document.getElementById('modal-title').textContent='검증 근거 · 2026.09.08 KST';body.innerHTML=`<h3>회귀 테스트</h3><p>격리된 PostgreSQL·Redis에서 전체 테스트 806개 통과. Starlette/httpx 사용 중단 예고 경고 1건. 프론트엔드 타입 검사·린트·프로덕션 빌드 통과.</p><h3>고정·가상 입력 평가</h3><p>33개: 의사결정 9개, AVM 1개, 계산기 16개, RAG 7개. 의사결정은 21개 상태를 재생합니다. AVM 기본 평가는 축약 추정기를 사용하는 가상 홀드아웃으로 전체 AVM 실거래 정확도가 아닙니다.</p><p><code>evaluation-results/20260907T151831Z-ec559aa4/results.json</code><br>실행 시각: 2026-09-07 15:18 UTC = 2026-09-08 00:18 KST</p><h3>검색·인용 지표</h3><p>시드 키워드 검색 k=4: Precision 0.2143, Recall 1.0000, MRR 0.7857, nDCG 0.9385. 정답 문서가 없는 질문은 Recall/nDCG 평균에서 제외하고 검색 결과 없음으로 평가합니다. Precision 분모는 반환 수가 아닌 k입니다.</p><p>답변·인용 검사 코드는 관련 근거, 계산 출력, 허용 수치, 폴백 등을 검사합니다. 실제 대화의 의미적 정확성·법령 최신성·문장별 근거 적합성을 완전히 검증했다는 주장은 하지 않습니다.</p><h3>브라우저 검증</h3><p>AVM은 후보 자동 채움, 채팅·폼 실행, 실제 결과 저장·단위 변환·재조회까지 확인했습니다. 자금은 Docker 프론트에서 실제 계산, 부족 자금 경고, 이전 입력 복원, 재계산, 희망가 변경 후 안내까지 확인했습니다.</p><p><code>scripts/verify_candidate_avm_browser.cjs<br>scripts/verify_candidate_funding_browser.cjs</code></p><h3>작성 기준</h3><p>README, 평가 보고서와 실제 코드·화면을 기준으로 구성했습니다. 화면은 가상 후보로 실행한 실제 제품 캡처이며 개인정보를 포함하는 계정 메뉴는 촬영 영역에서 제외했습니다. 검증용 계정은 삭제했습니다. 담당자 이름과 개인 기여 비율은 확인되지 않아 기재하지 않았습니다.</p>`;modal.showModal()};track();
